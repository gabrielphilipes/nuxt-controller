/**
 * Declare the environment variables that are used in the project, exactly as in the .env file
 *
 * It is not necessary to use suffix VITE_ in the name of the variable.
 */
export default interface EnvTypes {
    APP_NAME: string
}
